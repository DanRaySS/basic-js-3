const obj = {
    library: 'closed',
    cafe: 'open',
    boolean: true
};

const arr = [1, 2, 3, '', '45', true];

const { library } = obj;

const [first, second] = arr;

console.log('Test');
console.log(library);
console.log(first);
console.log(second);
